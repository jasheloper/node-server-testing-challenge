const db = require("../data/dbConfig.js");

module.exports = {
  create,
  destroy,
  readAll
};

async function create(hobbit) {
   return null;
}

function destroy(id) {
  return null;
}

function readAll() {
   return null;
}
